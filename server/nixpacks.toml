[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd .. && npm install && cd client && npm install && npm run build && cd ../server && npm install"]

[start]
cmd = "node src/index.js"
